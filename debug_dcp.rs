// Standalone script to debug DCP parsing
// Run with: rustc debug_dcp.rs -o debug_dcp && ./debug_dcp

fn main() {
    // We can't easily run this standalone, so let's add debug logging to the parser instead
    println!("Use 'cargo run' with the debug logging added to dcp.rs");
}
